export const PATCH = () => {
	// TODO: add patch for product
	return new Response(JSON.stringify({ message: 'success' }), { status: 200 });
};

export const DELETE = () => {
	// TODO: add patch for product
	return new Response(JSON.stringify({ message: 'success' }), { status: 200 });
};
